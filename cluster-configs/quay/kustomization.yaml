apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: quay-configs

resources:
- quay-operator.yaml

commonLabels:
  config.openshift.io/component: quay
  config.openshift.io/managed-by: gitops
