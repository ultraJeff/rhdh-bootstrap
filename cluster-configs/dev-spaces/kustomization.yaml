apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: dev-spaces-configs

resources:
- namespace.yaml
- operator-group.yaml
- subscription.yaml
- che-cluster.yaml

commonLabels:
  config.openshift.io/component: dev-spaces
  config.openshift.io/managed-by: gitops
