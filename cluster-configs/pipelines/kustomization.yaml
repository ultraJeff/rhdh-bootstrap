apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: pipelines-configs

resources:
- pipelines-operator.yaml

commonLabels:
  config.openshift.io/component: pipelines
  config.openshift.io/managed-by: gitops
