# Keycloak Integration Secrets for RHDH
#
# To use:
# 1. Copy this file to keycloak-secrets.yaml (it's gitignored)
# 2. Fill in your Keycloak configuration (base64 encoded values)
# 3. Apply with: oc apply -f keycloak-secrets.yaml
#
# IMPORTANT: KEYCLOAK_CLIENT_SECRET must match the 'secret' field in:
#   cluster-configs/keycloak/rhdh-realm-import.yaml (plain text value)
# If you change it here, update it there too!
#
# To base64 encode: echo -n "your-value" | base64
apiVersion: v1
kind: Secret
metadata:
  name: keycloak-secrets
  namespace: rhdh
type: Opaque
data:
  KEYCLOAK_BASE_URL: <base64-encoded-keycloak-url>        # e.g., https://sso.apps.your-cluster.domain
  KEYCLOAK_CLIENT_ID: <base64-encoded-client-id>          # e.g., rhdh
  KEYCLOAK_CLIENT_SECRET: <base64-encoded-client-secret>  # Must match realm import
  KEYCLOAK_LOGIN_REALM: <base64-encoded-realm>            # e.g., rhdh
  KEYCLOAK_REALM: <base64-encoded-realm>                  # e.g., rhdh
