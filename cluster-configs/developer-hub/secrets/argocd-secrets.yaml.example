# ArgoCD Integration Secrets for RHDH
#
# To use:
# 1. Copy this file to argocd-secrets.yaml (it's gitignored)
# 2. Fill in your ArgoCD credentials
# 3. Apply with: oc apply -f argocd-secrets.yaml
#
# Note: Remove trailing slashes from ARGOCD_URL or the plugin will throw "method not allowed" errors
apiVersion: v1
kind: Secret
metadata:
  name: argocd-secrets
  namespace: rhdh
type: Opaque
stringData:
  ARGOCD_URL: https://openshift-gitops-server-openshift-gitops.apps.<your-cluster-domain>
  ARGOCD_USERNAME: <argocd-username>
  ARGOCD_PASSWORD: <argocd-password>
